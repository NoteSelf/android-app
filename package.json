{
  "name": "noteself",
  "version": "1.0.0",
  "description": "noteself mobile apps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cordova run android --device",
    "dev-legacy":"cordova plugin add cordova-plugin-crosswalk-webview && cordova clean && npm run dev",
    "icons": "cordova-icon",
    "tw": "cross-env TIDDLYWIKI_PLUGIN_PATH=../../tiddlywiki/plugins_dist npm run TW",
    "TW": "tiddlywiki ./tw --verbose --build index boot",
    "TW:BOOT":"tiddlywiki ./tw --verbose --build boot"
  },
  "author": "Danielo Rodríguez Rivero",
  "license": "MIT",
  "devDependencies": {
    "cordova-icon": "^0.9.1",
    "cross-env": "^3.1.4"
  },
  "dependencies": {
    "cordova-custom-config": "^3.1.2"
  }
}
